import styled from "styled-components";

export const Container = styled.header`
    text-align: center;
    
    font-size: 1.3rem;
    letter-spacing: 1px;

    padding: 1.1rem;

    background-color: #000;

    a {
        color: #FFF;
        background-color: #000;
    }
`